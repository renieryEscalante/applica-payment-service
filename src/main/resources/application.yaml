spring:
  security:
    user:
      name: applica
      password: 4pl1c42026!
  application:
    name: applica-payment-service
  datasource:
    url: jdbc:h2:mem:applica
    username: sa
    password: 4pl1c42026!
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
server:
  servlet:
    context-path: /applica-payment-service